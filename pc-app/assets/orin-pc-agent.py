"""Legacy PC agent removed for security.

Do not expose or execute this historical script. Install the signed Orin Agent
release, which enforces explicit tool scopes, workspace boundaries, and approval
records.
"""

raise RuntimeError("legacy PC agent is disabled; install the signed Orin Agent release")
