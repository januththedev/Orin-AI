import { describe, expect, it } from "vitest";
import { intentFor, languageFor } from "../../api/_lib/chatV2.js";
describe("Chat v2 intent", () => { it("selects all canonical chains", () => { expect(intentFor("debug this typescript error", null).alias).toBe("orin-coding"); expect(intentFor("analyze and compare these architecture options", null).alias).toBe("orin-thinking"); expect(intentFor("hello", null).alias).toBe("orin-cheap"); expect(intentFor("tell me about your day", null).alias).toBe("orin-balanced"); expect(intentFor("anything", "orin-thinking").source).toBe("override"); }); it("detects supported languages", () => { expect(languageFor("hello", null)).toBe("en"); expect(languageFor("ඔයාට කොහොමද", null)).toBe("si"); expect(languageFor("நீங்கள் எப்படி இருக்கிறீர்கள்", null)).toBe("ta"); }); });
