"""This historical executable is intentionally unavailable.

The previous public script accepted commands/code and invoked a host shell or
dynamic exec. There is no public execution path pending the Orin-controlled
sandbox security gate.
"""

raise RuntimeError("public code execution is disabled")
